<% content_for :title, "New region" %>

<div class="w-full max-w-xl mx-auto">
  <%= render "admin/shared/nav" %>
  <h1 class="text-3xl font-semibold text-gray-900">New region</h1>
  <%= render "form", resource: @resource, url: admin_regions_path %>
</div>
