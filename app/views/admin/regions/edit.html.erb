<% content_for :title, "Edit region" %>

<div class="w-full max-w-xl mx-auto">
  <%= render "admin/shared/nav" %>
  <h1 class="text-3xl font-semibold text-gray-900">Edit region</h1>
  <%= render "form", resource: @resource, url: admin_region_path(@resource) %>
</div>
